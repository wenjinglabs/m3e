---
export function getStaticPaths() {
  const previews = import.meta.glob("../../demos/**/*.html", {
    query: "?raw",
    import: "default",
    eager: true,
  }) as Record<string, string>;
  return Object.entries(previews).map(([path, html]) => {
    const name = path.replace("../../demos/", "").replace(/\.html$/, "");
    const [lang, component, index] = name.split("/");
    return { params: { name }, props: { html, component, index } };
  });
}

const { html, component, index } = Astro.props;
---

<!doctype html>
<html lang="zh-CN">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <meta name="robots" content="noindex" />
    <title>{component} · 示例 {index} · M3E</title>
    <link rel="preload" href="/fonts/roboto.css" as="style" onload="this.onload=null;this.rel='stylesheet'" />
    <link rel="preload" href="/fonts/sym-outlined.css" as="style" onload="this.onload=null;this.rel='stylesheet'" />
    <noscript>
      <link rel="stylesheet" href="/fonts/roboto.css" />
      <link rel="stylesheet" href="/fonts/sym-outlined.css" />
    </noscript>
    <script type="module" src="/m3e/all.min.js"></script>
    <style>
      body {
        margin: 0;
        padding: 24px;
        font-family: "Roboto", system-ui, sans-serif;
        background: var(--md-sys-color-surface, #fef7ff);
        color: var(--md-sys-color-on-surface, #1c1b1e);
      }
    </style>
  </head>
  <body>
    <m3e-theme>
      <div set:html={html}></div>
    </m3e-theme>
  </body>
</html>
