---
title: "Slide Group 滑动组"
description: "m3e-slide-group 组件提供方向性分页控件,用于浏览溢出的内容。"
summary: "m3e-slide-group 组件提供方向性分页控件,用于浏览溢出的内容。"
script: ""
section: components
category: "navigation"
order: 40
---

<p> <code>m3e-slide-group</code> 组件提供方向性分页控件,用于浏览溢出的内容。 </p>

```js
import "m3e/slide-group";
```

## 用法

### 基础用法

<p> 要使用 <code>m3e-slide-group</code>,只需把内容嵌套在其中。当内容溢出时,会出现方向性分页按钮,让用户可以点击以滚动其内容。 </p>

<Demo name="zh/components/slide-group/0" />

### 方向

<p> 默认情况下,滚动组为水平方向。使用 <code>vertical</code> 特性可创建垂直方向的滚动组。 </p>

<Demo name="zh/components/slide-group/1" />

### 特性

<p><code>disabled</code>：是否禁用滚动按钮。</p>
<p><code>next-page-label</code>：下一页按钮的无障碍标签。</p>
<p><code>previous-page-label</code>：上一页按钮的无障碍标签。</p>
<p><code>threshold</code>：开始显示翻页控件的滚动阈值（像素）。</p>
